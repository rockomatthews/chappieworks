# Chappie Studio Standup — 2026-07-17

## Shipped
- `e40d16e` — Chappie can laugh. New `chappie-laugh.glb` (202 KB) wired into `ChappieScene.tsx` + `ShowcaseClient.tsx`; hero button on `/website` now triggers the 3D animation. First personality moment on the B2B surface. Receipt: the file is in `public/models/`.

## In flight
- **Site-wide auth + /studio hub** (Forge) — Supabase magic-link, wallet connect, super-user pill all shipped. Open tail: per-user project listing and admin bypass form need Phase 2 Postgres move. Unblocked but parked — Paperclip lands first.
- **Multica** (Forge) — Phase 1 artifacts done. Docker compose, daemon image, ChappieTeam squad JSON, test issues all written. Waiting on Sire to run the 6-step SIRE_DEPLOY.md and get the squad live on Render ($28/mo).

## Blocked / needs Sire
- **Multica deploy** — SIRE_DEPLOY.md is sitting there. Six steps. $28/mo. Sire runs it, the studio gets a real task board.
- **ClawBazaar scoping** — JS-rendered, needs an authenticated browser pass. Can't scope it headless.
- **@ridark_eth opportunity** — paywalled on X. Paste the gist or open it in a live browser session.

## Next before Friday
1. **Paperclip integration** — makes the queue a live read instead of a hand-maintained TSX constant. Directly qualifies buyers (they see real studio velocity). This is the "company configuration goes live in Paperclip" milestone on `/studio/finance`.
2. **Multica deploy** — Sire's six steps. Once the squad is live, every standup is routed through the board instead of this file.
