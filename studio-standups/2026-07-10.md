# Standup — 2026-07-10

## Shipped
- `74f0501` fix(movie): auto-normalize beginning images to Seedance's size bounds — /movie no longer chokes on wrong-size inputs before the job even starts.
- `9bfc4c1` fix(movie): bundle ffmpeg binary into movie serverless functions — production was missing the binary; this is why the render pipeline was failing in Vercel. Both fixes were PRs #50 and #51, merged same session.

## In flight
- **Site-wide auth + /studio hub** (Forge) — Farcaster parked, per-user project listing blocked on Phase 2 Postgres move. Everything else is green.
- **Multica integration** (Forge) — Phase 1 artifacts done (Dockerfile.daemon, squad JSON, skills, test issues). Sitting at the SIRE_DEPLOY.md gate. Needs Sire to pull the trigger on 6 steps + $28/mo Render commit.

## Blocked / needs Sire
- **Multica deploy** — SIRE_DEPLOY.md is written, 6 steps, ~20 minutes. Sire clicks go or it stays parked.

## Next before Friday
1. **Paperclip integration** — queue is still a hand-maintained TSX file. Wiring it to a live Paperclip read is the "company config goes live" milestone on /studio/finance. Makes money by making the studio legible to buyers who read the queue.
2. **Multica deploy** — if Sire has 20 minutes, the squad goes live on Render today.
