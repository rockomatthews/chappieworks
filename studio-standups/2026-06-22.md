# Standup — 2026-06-22

## Shipped
- 3D Chappie showcase lab at `/website/showcase` — WebGL shader hero + live React Three Fiber model, linked from the `/website` CTA. Receipt: `00618f9`.

## In flight
- **Site-wide auth + /studio hub + wallet connect** (Forge, Vault gating) — magic-link, wallet panel, super-user/admin pills all shipped. Open: Farcaster sign-in (parked), per-user "Your projects" listing (needs Phase 2 Postgres).
- **Multica integration** (Forge) — Phase 1 artifacts done (Dockerfile, squad JSON, deploy doc). Sitting on Sire's 6-step deploy.

## Blocked / needs Sire
- Multica deploy is sitting on Sire running `SIRE_DEPLOY.md` — GitHub App, 4 Render services, 8 env vars, import squad JSON. Nobody else can push this forward.

## Next before Friday
1. **Bench QA pass on `/website/showcase`** before it does its job as a portfolio flex — it shipped yesterday, untested on real devices. A buyer hitting a broken 3D model on mobile is worse than no showcase at all.
2. **Close the open coin commitments** — CHA-5 pay-from-stake router and the clanker.world metadata edit (website + twitter) are public promises from the launch thread still sitting open. Money exposure if we let a public promise rot.

Money or it didn't happen — one commit landed, receipt above. Rest is queue bookkeeping.
