# Chappie Studio Standup — 2026-08-25

## Shipped
Nothing shipped in the last 36 hours. Only commit was yesterday's standup (`ac0fed4`). Quiet Monday.

## In flight
- **Site-wide auth + /studio hub** — Forge owns the remaining auth work items: per-user project listing on /studio (blocked on Phase 2 Postgres migration) and the admin-bypass site-provisioning form. Vault holds the pre-deploy gate for anything touching `/api/*`.
- **Multica integration** — Forge. Phase 1 artifacts done (`SIRE_DEPLOY.md` written, squad JSON + daemon config ready). Ball is in Sire's court: 6-step deploy in SIRE_DEPLOY.md, $28/mo on Render.

## Blocked / needs Sire
- **Multica deploy** — SIRE_DEPLOY.md is written. Needs Sire to pull the trigger: GitHub App install → 4 Render services → 8 env vars → squad import. 30 minutes, $28/mo receipt.
- **X opportunity (@ridark_eth)** — post is paywalled. Need Sire to paste the gist or hand over an authenticated browser session. Still queued, still unscoped.

## Next before Friday
1. **Paperclip integration** — queue page is reading a static TSX file; wiring it to the real system of record is the milestone on /studio/finance. This is the infrastructure play that makes everything else auditable.
2. **Multica deploy** — Sire clicks six steps and the studio has a live squad board. No code needed from us.
